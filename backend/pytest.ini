[pytest]
testpaths = tests
asyncio_mode = auto
filterwarnings =
    ignore:Using `httpx` with `starlette.testclient`
markers =
    slow: marks tests as slow (real LLM calls; run with: pytest -m slow)
addopts = -m "not slow"
