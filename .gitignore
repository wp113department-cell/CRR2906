node_modules/
.next/
dist/
build/
.turbo/
*.tsbuildinfo

# env / secrets — never committed, enforced also by the Policy Engine
.env
.env.*
!.env.example
secrets/

# worktrees created by agent-runtime for isolated coding tasks
.worktrees/

# editor / OS
.DS_Store
.vscode/*
!.vscode/extensions.json

# logs
*.log
npm-debug.log*
pnpm-debug.log*

# postgres docker volume
.pgdata/
